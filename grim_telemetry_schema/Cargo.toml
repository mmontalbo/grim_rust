[package]
name = "grim_telemetry_schema"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
name = "grim_telemetry_schema"
path = "src/lib.rs"
