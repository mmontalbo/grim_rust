[package]
name = "grim_analysis"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
libc = "0.2"
grim_telemetry_schema = { path = "../grim_telemetry_schema" }
serde_json = "1"
